------creating database mysql-------
lab container-create start 

-----> mengecek init--------
sudo podman images 

-------------menjalankan container---------
sudo podman run --name mysql-basic1 \
> -e MYSQL_USER=user1 -e MYSQL_PASSWORD=mypa55 \
> -e MYSQL_DATABASE=items -e MYSQL_ROOT_PASSWORD=r00tpa55\
> -d rhscl/mysql-57-rhel7:5.7-3.14  -----------------> "-id = berjalan di background"


--ngecek lagi container yg berjalan ----
sudo podman ps
sudo podman ps -a -> berjalan mau tidak berjalan
sudo podman ps --format "{{.ID}} {{.Image}} {{.Names}}" ->> nampilin yg mau di tampilin saja

---masuk ke terminal container--
sudo podman exec -it mysql-basic1 /bin/bash

--melihat daftar nama user yg ada di container---
sudo podman exec -it mysql-basic1 cat /etc/passwd

--- masuk ke sql --
sudo podman exec -it mysql-basic1 /bin/bash
>mysql -uroot



--finish container--
lab container-create finish

--ngecek--
lab container-review start

--ambil quay.io--
sudo podman -d -p 8080:80 --name httpd-basic quay.io/redhattraining/httpd-parent:2.4

---masuk untuk melihat akses untuk updat di server---
sudo podman exex -it httpd-basic /bin/bash

bash> cd /etc/httpd/conf
bash> grep DocumentRoot httpd.conf

---ubah halaman awal index--
vi /var/www/html/index.html












